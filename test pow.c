#include <stdio.h>
#include <math.h>

int main(){
    int x, z, y, g=1, a, b;

    printf("what is the rate of increase, the first number and how many numbers?\n");
    scanf("%d %d %d", &x, &y, &a);


    while(b<a){
            pow
        z=y*x^(g-1);
        b++;
    }

    printf("%d", z);
}
